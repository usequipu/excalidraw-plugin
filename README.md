# excalidraw-plugin

Excalidraw canvas plugin for [Quipu](https://quipu.cc). Infinite whiteboard editor for `.excalidraw` files.

## Install

Open the Plugin Manager panel in Quipu → Available → Install.

## Supported file types

`.excalidraw`

## Build from source

```bash
npm install
npm run build
```
